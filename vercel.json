{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf api/app api/data && cp -r backend/app api/app && cp -r backend/data api/data",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
